# AGENTS.md

> 本文件供支持 `AGENTS.md` 约定的 AI 编程工具读取。
> 如果你正在使用本仓库来完成任务，请遵循以下要点。

---

## 本项目是什么

`agy-sub-agent` 是一份**把子任务外派给 Antigravity CLI（`agy`）的方法与技能**。

它不是一个软件库，而是一套**经过实测验证的操作规范与判据**。当你的任务满足「**可独立打包、可独立验收**」时，可以考虑外派给 `agy` 执行。

---

## 何时使用本仓库

**适合外派**：

- 独立检索（联网搜集信息）
- 独立脚本 / 代码生成
- 交叉验证、第二意见
- 方案评审
- 独立数据核对

**不适合外派**：

- 依赖当前会话隐式共识的任务（摘要必丢关键约束，外部结论**比不派更危险**）
- 任务极简但上下文极大（每轮 ≈28.7k 固定开销）
- 含非公开信息（须先询问用户）

---

## 必须遵守的四条硬要求

调用 `agy` 时，这四条**不可违背**：

| # | 要求 |
|---|---|
| 1 | **受限网络环境必须设置 `HTTPS_PROXY` / `HTTP_PROXY` 环境变量**（`agy` 只读环境变量，不读系统代理） |
| 2 | **不要传 `--print-timeout`**（默认即不限时长；传非零值会截断长任务并产生假成功） |
| 3 | **失败必须自动重试**（同一任务串行重试 ≥3 次） |
| 4 | **并发用「进程内 subagent」**；外部多进程最多 2 路，≥3 路禁止 |

---

## 必须遵守的验收纪律

### 判成功：三层判据

**不要只看 `status` 或退出码** —— 失败时它们同样返回 `SUCCESS` / `0`。

1. **调用层** —— `usage.total_tokens > 0`（唯一硬指纹）
2. **交付层** —— 要求的内容全部出现，且未停在中间态
3. **内容层** —— 结构完整 + 来源核验

### 检索类任务：必须逐条核验

实测中，一次文献检索任务**抽查 6 条来源有 3 条不实**（含**编造的 DOI，格式完整到肉眼无法分辨**）。

⇒ **凡引用必逐条实查**（`https://doi.org/<doi>`），未通过核验的条目**不得进入交付物**。
⇒ 派发检索任务时，**把禁止拼凑条款原文写进 prompt**（模板见 [`docs/verification.md`](docs/verification.md) §5.5）。

### 有副作用的操作：系统层回检

**不要采信它的自述。** 它可能声称"已成功创建"而实际没有。

⇒ 一律用 `ls` / `test -f` / `git status` 等**实际核验**。
⇒ 注意：**空回复 ≠ 失败** —— 操作可能已经执行了。

---

## 🚫 红线

**不派发、不授权任何删除类操作**（`rm` / `del` / `Remove-Item` / `shutil.rmtree` 等）。

原因：删除走 shell 而命令授权**只匹配命令串、没有路径感知**，实测中一条未锚定的正则模式删除了授权目录外的文件；且存在「**已真删但回复为空**」的假失败；删除不可逆。

**需要删除时**：由用户本人在本地执行，或改用作用域受限、可追溯的专用工具。

---

## 参考文档

| 文档 | 内容 |
|---|---|
| [`SKILL.md`](SKILL.md) | 完整技能规范（含全部细节） |
| [`docs/permissions.md`](docs/permissions.md) | 权限模型、最小授权、文件操作实测 |
| [`docs/verification.md`](docs/verification.md) | 三层判据、失败形态、内容核验 |
| [`docs/concurrency.md`](docs/concurrency.md) | 并发策略与实测梯度数据 |
| [`docs/troubleshooting.md`](docs/troubleshooting.md) | 按症状排障 |
| [`EVIDENCE.md`](EVIDENCE.md) | 所有结论的实测来源 |

---

## 版本注意

`agy` 迭代快、**默认值会变**。若本仓库描述与本机 `agy --help` 实列冲突，**以实列为准**。
