# 实测证据

> 本文记录本项目各项结论的**实验条件与观测数据**。
> 目的：让结论可被追溯、可被复现、可被推翻。
> **原则：区分「实测」与「推测」—— 本文件只记录实测。**

---

## 实验环境

| 项 | 值 |
|---|---|
| CLI | Antigravity `agy` |
| 版本 | **1.2.9**（部分早期数据取自 1.2.7） |
| 实测日期 | 2026-09-21 ～ 2026-09-24 |
| 主要模型 | `gemini-3.8-flash-high` |
| 平台 | Windows（Git Bash） |
| 网络 | 经本地 HTTP 代理（环境变量 `HTTPS_PROXY`/`HTTP_PROXY`） |

> ⚠️ **版本敏感性**：`agy` 迭代较快。若你的版本与上述不同，部分结论（尤其默认值）可能已变化。
> **复现前请先执行 `agy --version` 与 `agy --help`。**

---

## E1. 并发梯度实验

**目的**：确定外部多进程并行的安全上限。

**方法**：同时启动 N 个 `agy` 进程，每个执行独立任务，统计成功数。

| 并发路数 | 成功 / 总数 | 备注 |
|---|---|---|
| 2 路 | **2 / 2** | ✅ |
| 3 路 | 2 / 3 | 出现失败 |
| 5 路 | **0 / 5** | 全部失败 |
| 5 路（**隔离工作目录**） | 1 / 5 | **仍然失败** |

**失败形态**：

```
400 INVALID_ARGUMENT
tools[…].properties[period].enum[0]: cannot be empty
retryable: false
```

**结论**：

1. 外部多进程并行 **≥3 路会概率性失败**，应限制在 **最多 2 路**。
2. **隔离工作目录无法规避** ⇒ 竞态发生在**全局层**（MCP 状态缓存），而非工作目录层。

**后续修正**：早期把该失败完全归因于"多进程争抢"。后续在**严格串行**条件下复测 14 次，仍有 **3 次（约 8%）** 命中同一错误 ⇒ **串行只降低概率、不免疫**。正确姿态为「**串行 + 失败自动重试 ≥3 次**」。

---

## E2. 进程内 subagent 可用性

**目的**：验证官方推荐的并行方式是可行的。

**方法**：单个 prompt 内要求 fan out 三个独立子任务并汇总。

| 项 | 观测 |
|---|---|
| 结果正确性 | ✅ 三路汇总全对 |
| 耗时 | 约 **33 s**（简单任务）；约 **400 s**（3 路检索类） |
| 进程数 | **1** |

**可审计指纹**（证实确实 fan out）：

- `conversations/` 下新增多个 `.db`（1 主 + N 子）
- 主库存在载荷含 `invoke_subagent` 的 `type=132` step

**结论**：进程内 subagent **实测可用**，且是并发首选方式。

---

## E3. 检索类任务来源失真率

**目的**：评估外派检索类任务的结果可信度。**这是本项目最重要的一项实验。**

**方法**：派发一次文献调研任务，产出报告后**抽查 6 条引用来源**，逐条实查 DOI / 数据页面。

| # | 形态 | 实查结果 |
|---|---|---|
| ① | 声称的 IEEE 期刊论文 | **DOI 在系统中不存在**（2 条） |
| ② | 声称的数据集 | **DOI 真实，但内容被整体改写**：实为 135 KB 工程文件，被描述成「12,852 组时序 + 50 维特征 + 已清洗矩阵」；许可 **CC0 → CC BY**（1 条） |
| ③ | 其余 | 3 条未发现明显问题 |

**失真率：3 / 6 = 50%。**

**对照实验**：同一任务改用其他模型通道执行，抽查 **3 / 3 全部真实**，数值细节与原文逐字吻合（含编号、时长等具体参数）。

**结论**：

1. 该通道在**检索类任务上存在系统性来源失真**。
2. **编造的 DOI 格式完整、卷期页齐全，肉眼无法分辨** —— 无法通过"看起来专业"来判断真伪。
3. ⇒ **凡引用必逐条实查**，不能依赖抽样，也不能依赖观感。

> **说明**：本项的"电力/工程"背景已做匿名化处理，不影响结论的通用性。

---

## E4. 权限被拒的成本

**目的**：量化"失败探测"的代价。

**方法**：在缺少所需授权的情况下派发需要写文件 / 执行命令的任务，记录计费。

| 场景 | 观测 |
|---|---|
| 权限被拒的单次调用 | 输入 token **3.0 万 ～ 18.2 万** |
| 其中 `write_file` 被拒一次 | **18.2 万** |
| 返回状态 | `status=SUCCESS`、`exit=0`、`total_tokens>0`、**`response` 为空串** |

**结论**：

1. **失败的轮次同样计费**，且成本不低。
2. ⇒ 派真任务前应做**最低成本的桩验证**。
3. ⇒ 空回复不能作为"未执行"的判据（见 E5）。

---

## E5. 「空回复 ≠ 失败」的假失败

**目的**：验证空回复与操作实际执行之间的关系。

**观测**：某步骤**成功执行**（文件确实写入），但紧随其后的**自检命令**不在授权范围内 → 被拒 → **整个回合返回空 `response`**。

**结论**：

> **空回复既不等于失败，也不等于成功。**

⇒ 任何有副作用的操作，**成败一律在系统层核验**（`ls` / `test -f` / `git status`），不采信回复内容。

---

## E6. 超时默认值的版本变更

**目的**：确定当前版本的 headless 默认超时行为。

**方法**：三种独立来源交叉验证。

| 来源 | 结果 |
|---|---|
| 厂商 changelog（1.2.6 段） | *"Changed the default timeout for headless runs **from 5 minutes to unlimited**"* |
| 本机 `agy --help`（1.2.9） | `--print-timeout ... **0 waits until the turn completes (default 0s)**` |
| 第三方公开资料 | 多数写"默认 5m" —— **追溯其自述验证版本为 v1.1.12**，属旧版本 |

**结论**：**≤1.2.5 默认 5 分钟；≥1.2.6 改为不限时长。**

**衍生教训**：网传资料常滞后于版本。`agy` 的权威来源是**本机 `--help`**，不是任何第三方文档（包括本项目）。

---

## E7. 尝试构造长耗时任务（失败的实验，记录备查）

**目的**：试图构造超过 5 分钟的调用，以直接验证超时默认值。

**四次尝试**：

| # | 任务设计 | 实际耗时 |
|---|---|---|
| A | 空闲计时 7 分钟 | 58 s —— 因缺 `command` 权限，只输出"将要做"的声明后结束 |
| B | 写 40 章 × 400 字 | 266 s，全文完整 |
| C | 写 120 章 × 500 字 | 158 s —— **只给目录 + 4 章正文**，建议"分卷推进" |
| D | 12×12 矩阵逐格平方（144 格） | 52 s，计算正确 |

**结论**：**无法通过构造复杂任务来稳定逼近超时上限** —— 该模型的推理速度使得常规任务难以达到分钟级。

**方法论记录**：构造"长耗时任务"来控制实验时长**不可行**；此类验证应改用**权威文档比对**（如 E6）。

---

## E8. 权限与文件操作能力

**目的**：确定本地文件读写的实际能力边界。

**方法**：派单实测 + 磁盘回检验证。

| 操作 | 结果 | 工具 |
|---|---|---|
| 读取 | ✅ 成功 | `view_file` |
| 创建 | ✅ 成功 | `write_to_file` |
| 修改（全量覆写） | ✅ 成功 | `write_to_file` |
| 修改（局部编辑） | ✅ 成功 | `replace_file_content` |
| 删除 | ❌ **原生不具备** | 无 |

**关键观测**：

1. **原生无删除工具** —— 只有 `view_file` / `write_to_file` / `replace_file_content`。
2. **`--add-dir` 是最简授权** —— 清空所有 allow 规则后，仅加 `--add-dir <目录>` 即可在该目录内创建文件。
3. **权限动作名 ≠ 工具名** —— 工具 `write_to_file` 对应的规则名是 `write_file`。
4. **通配符无效** —— 目录级必须用**末尾斜杠**。
5. **必须绝对路径** —— 相对路径被前置校验拒绝；其"当前目录"是受信工作区根，非调用方 cwd。
6. **`settings.json` 会被改写**，且空数组 `"allow": []` 会被整体丢弃 → 规则**静默失效**。

---

## E9. shell 命令授权的作用域

**目的**：验证命令授权是否等于开放 shell。

| 观测 | 结果 |
|---|---|
| 只授权命令 A，执行未授权的命令 B | ❌ **被拒** |
| `command(regex:<模式>)` 是否可用 | ✅ 可用 |
| 正则是否具备路径感知 | ❌ **完全没有** |

**关键风险实证**：一条未锚定的模式 `command(regex:.*Remove-Item.*)` **成功删除了授权目录之外的文件**。

**结论**：

1. 命令授权**按模式强制作用域**，不等于全量开放。
2. 但**正则是减速带，不是安全边界** —— 由于没有路径感知，它能触达授权目录之外。
3. ⇒ **不做、不授权、不派发任何删除类操作。**

---

## E10. 版本一致性核对

**目的**：确认本机 CLI 的实际行为与文档描述是否一致。

| 项 | 观测 |
|---|---|
| `agy --version` | 1.2.9 |
| `--print-timeout` 默认值 | `0s`（不限时长），与 changelog 1.2.6 一致 |
| 技能/插件目录 | 技能来自 `~/.gemini/config/plugins/{插件}/skills/`，**不是** `~/.gemini/antigravity-cli/skills/` |
| `agy plugin list` | 输出"无" —— 但插件实际可用（该命令只列 `plugin import` 迁入的） |
| `agy mcp list` | 输出"无" —— 但插件级 MCP 实际生效（该命令只列 CLI 级） |

**结论**：两个 `list` 命令的**输出不能作为"是否生效"的判据**。

---

## 数据可信度说明

| 类别 | 说明 |
|---|---|
| **样本量** | 部分实验样本较小（如 E1 的并发梯度、E3 的 6 条来源抽查）。结论应视为**方向性指引**，而非精确统计量 |
| **环境依赖** | 所有数据来自单一环境（特定版本、单一平台、特定网络）。跨环境可能存在差异 |
| **时效性** | `agy` 迭代快，行为可能已变化。**复现前请先核对版本** |
| **推测标注** | 本文不含推测。任何未实测的内容，在本项目的其他文档中均已明确标注"推测"或"待核" |

---

## 如何贡献新证据

欢迎补充。提交时请包含：

1. **`agy --version`** 的精确输出
2. **完整可复现的命令**（含所有参数）
3. **原始观测结果**（而非结论摘要）
4. **样本量**（若为多次实验）

若某项旧结论与你的实测不符，请**直接指出并附证据** —— 本项目的价值在于准确性，不在于维护既有结论。
